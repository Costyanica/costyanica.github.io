// const buttons = document.querySelectorAll('.button');

// const watchMore = (item) => {
//     item.toElement.classList.add('active');
// }


// const hideAll = (item) => {
//     item.toElement.classList.remove('active');
// }


// buttons.forEach((item) => item.addEventListener('mouseover', watchMore));

// buttons.forEach((item) => item.addEventListener('mouseout', hideAll));